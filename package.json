{
  "name": "react-ts-component-generator",
  "version": "1.0.0",
  "description": "generates directories with react components, index, and stylesheet files inside",
  "main": "index.js",
  "scripts": {
    "genComp": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tareq",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.0.0"
  },
  "bin": {
    "gencomp": "./index.js"
  }
}
